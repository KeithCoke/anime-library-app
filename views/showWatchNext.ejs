<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ('./partials/head.ejs') %>
        <link rel="stylesheet" href="/public/styles/" />
</head>

<body>
        <div class="flex-container">
            <a href="/"><button>Home</button></a>
            <form action="/watchList/<%= watchList._id %>?_method=DELETE" method="POST">
                <button type="submit">Delete watch</button>
            </form>
            <a href='/watchList/<%= watchList._id %>/edit'><button>Edit watch</button></a>
        
            <div class="flex-container">
                <ul>
                    <% for (let watchList of watchLists) { %>
                    <li>
                        <a href="/watchList/<%= watchList._id %>">
                            <div class="index-show-watchList">
                                
                                <h1><%= watchList.title %></h1>
                                <h3><%= watchList.description %></h3>
                            </div>
                        </a>
                    </li>
                    <% } %>
                </ul>
            </div> -->

        </main>
</body>

</html>